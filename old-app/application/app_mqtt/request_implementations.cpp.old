
// #include "app.h"

// #include "pal_logger.h"

// #include "board.h"

// #define __TAG__  "APP_RQST"

// int32_t on_reboot_requested(const char * payload, char *response_payload){
    
//     int32_t ret = ERROR_OK;
//     do{

//         if(payload == NULL){
//             ret = ERROR_APP_NULL;
//             break;
//         }

//         JsonDocument doc;
//         DeserializationError error = deserializeJson(doc, payload);
//         if (error) {
//             // Return an error message if parsing fails
//             LOG_MSG_DEBUG(LOG_EN, "Error parsing JSON: %s", error.c_str());
//             ret = ERROR_APP_BUSY;
//             break;
//         }
        
//         uint32_t msg_idx = doc["msg_idx"];
//         JsonObject data = doc["data"];

//         int32_t ret = ERROR_OK;
//         if(doc["data"]["password"].is<String>()){ 
//             String password = doc["data"]["password"].as<String>();
//             std::string uuid;
//             // ret = app_config_get_uuid(&uuid, 1000);
//             // if(ret == ERROR_OK){
//             //     if(strcmp(password.c_str(), uuid.c_str()) == 0){
//             //         LOG_MSG_DEBUG(LOG_EN, "Rebooting the board");
//             //         ret = ERROR_OK;
//             //         board_restart();
//             //     }
//             //     else{
//             //         LOG_MSG_DEBUG(LOG_EN, "Reboot password is incorrect ");
//             //         ret = ERROR_APP_BUSY;
//             //     }
//             // }
//         }

//     }while(false);

//     return ret;
// }


// int32_t on_get_requested(const char * payload, char *response_payload){
    
//     int32_t ret = ERROR_OK;
//     do{

//         if(payload == NULL){
//             ret = ERROR_APP_NULL;
//             break;
//         }

//         JsonDocument doc;
//         DeserializationError error = deserializeJson(doc, payload);
//         if (error) {
//             // Return an error message if parsing fails
//             LOG_MSG_DEBUG(LOG_EN, "Error parsing JSON: %s", error.c_str());
//             ret = ERROR_APP_BUSY;
//             break;
//         }
        
//         uint32_t msg_idx = doc["msg_idx"];
//         char version[20];
        

//         JsonObject data = doc["data"];

//         int32_t ret = ERROR_OK;
//         if(doc["data"]["password"].is<String>()){ 
//             String password = doc["data"]["password"].as<String>();
//             std::string uuid;
//             // ret = app_config_get_uuid(&uuid, 1000);
//             // if(ret == ERROR_OK){
//             //     if(strcmp(password.c_str(), uuid.c_str()) == 0){
//             //         LOG_MSG_DEBUG(LOG_EN, "Rebooting the board");
//             //         ret = ERROR_OK;
//             //         board_restart();
//             //     }
//             //     else{
//             //         LOG_MSG_DEBUG(LOG_EN, "Reboot password is incorrect ");
//             //         ret = ERROR_APP_BUSY;
//             //     }
//             // }
//         }

//     }while(false);

//     return ret;
// }

// int32_t on_set_requested(const char * payload, char *response_payload){
    
//     int32_t ret = ERROR_OK;
//     do{

//         if(payload == NULL){
//             ret = ERROR_APP_NULL;
//             break;
//         }

//         JsonDocument doc;
//         DeserializationError error = deserializeJson(doc, payload);
//         if (error) {
//             // Return an error message if parsing fails
//             LOG_MSG_DEBUG(LOG_EN, "Error parsing JSON: %s", error.c_str());
//             ret = ERROR_APP_BUSY;
//             break;
//         }
        
//         uint32_t msg_idx = doc["msg_idx"];
//         JsonObject data = doc["data"];

//         int32_t ret = ERROR_OK;
//         if(doc["data"]["password"].is<String>()){ 
//             String password = doc["data"]["password"].as<String>();
//             std::string uuid;
//             // ret = app_config_get_uuid(&uuid, 1000);
//             // if(ret == ERROR_OK){
//             //     if(strcmp(password.c_str(), uuid.c_str()) == 0){
//             //         LOG_MSG_DEBUG(LOG_EN, "Rebooting the board");
//             //         ret = ERROR_OK;
//             //         board_restart();
//             //     }
//             //     else{
//             //         LOG_MSG_DEBUG(LOG_EN, "Reboot password is incorrect ");
//             //         ret = ERROR_APP_BUSY;
//             //     }
//             // }
//         }

//     }while(false);

//     return ret;
// }